<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mood Tracker</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Courgette&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet"/>
    <style>
        :root {
            --primary-color: #BBE1FA;
            --secondary-color: #38aba0;
            --text-color: #3A3A3A;
        }

        body {
            background: var(--primary-color);
            color: var(--text-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .container {
            width: 50%;
            background: rgba(255, 255, 255, 0.6);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
            border: 2px solid rgba(255, 255, 255, 0.3);
            padding: 2rem;
            text-align: center;
        }

        .glassmorphism {
            background: rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
            border: 2px solid rgba(255, 255, 255, 0.3);
            padding: 2rem;
        }

        .header {
            background: var(--secondary-color);
            padding: 1rem;
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
            border-radius: 12px;
        }

        .mood-btn {
            font-size: 2rem;
            padding: 0.8rem;
            border-radius: 12px;
            border: none;
            cursor: pointer;
            transition: 0.3s;
        }

        .mood-btn:hover {
            transform: scale(1.1);
        }
        h1{
        font-family: "Courgette", serif;
        font-weight: 400;
        font-style: normal;
        }
    </style>
</head>
<body>
    <nav class="w-[98.3vw] h-16 bg-[#38aba0] flex items-center fixed top-0 left-0 shadow-lg shadow-gray-400 glassmorphism mt-[0.8vw] ml-[0.8vw] justify-between">
        <h1 class="text-white text-2xl font-bold ">TRIPTI</h1>
        
        <div class="border border-gray-300 rounded-full p-1 shadow-md shadow-gray-700 bg-[#CCF4EC] text-black flex gap-[3vw]">
            <a href="/journal/add" class="nav-link relative px-4 py-2 rounded-full font-medium transition-all duration-300 hover:bg-[#38aba0]/20 hover:border-2 hover:border-[#38aba0] hover:scale-105 hover:shadow-md">
                Journal
                <span class="nav-underline"></span>
            </a>
            <a href="/" class="nav-link relative px-4 py-2 rounded-full font-medium transition-all duration-300 hover:bg-[#38aba0]/20 hover:border-2 hover:border-[#38aba0] hover:scale-105 hover:shadow-md">
                Home
                <span class="nav-underline"></span>
            </a>
            <a href="/mood/add" class="nav-link relative px-4 py-2 rounded-full font-medium transition-all duration-300 hover:bg-[#38aba0]/20 hover:border-2 hover:border-[#38aba0] hover:scale-105 hover:shadow-md">
                MoodLog
                <span class="nav-underline"></span>
            </a>
            <a href="http://localhost:3000/forum/" class="nav-link relative px-4 py-2 rounded-full font-medium transition-all duration-300 hover:bg-[#38aba0]/20 hover:border-2 hover:border-[#38aba0] hover:scale-105 hover:shadow-md">
                Community
                <span class="nav-underline"></span>
            </a>
        </div>

        <form action="/user/logout" method="GET" class="inline">
            <button type="submit" class="bg-red-500 px-4 py-2 rounded-full text-white"><i class="ri-logout-circle-r-line"></i></button>
        </form>
      
    </nav>

    <div class="container mt-20 h-[45vw]">
        <div class="header">😀 MoodLog</div>
        <p class="mt-4">Track your mood daily and view trends.</p>
        <form id="moodForm" class="mt-6 flex justify-center gap-4">
            <button type="button" class="mood-btn bg-green-200" data-mood="happy">😊</button>
            <button type="button" class="mood-btn bg-yellow-200" data-mood="neutral">😐</button>
            <button type="button" class="mood-btn bg-orange-200" data-mood="sad">😔</button>
            <button type="button" class="mood-btn bg-red-200" data-mood="very_sad">😢</button>

            <div class="mt-6">
                <canvas id="moodChart" class=" h-[25vw]"></canvas>
            </div>
        </form>
        
        

    </div>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/mood.js"></script>
</body>
</html>
